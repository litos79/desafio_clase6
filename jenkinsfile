pipeline {
    agent any 

    stages {
        stage ('Install Dependencies') {
            steps {
                sh 'npm install'
            }
        }

              stage('Unit Test') {
            steps {
                echo 'Ejecutando Unit Test'
            }
        }

              stage('Build Application') {
            steps {
                bat 'npm run build'
            }
        }
           
           stage('Deploy') {
            steps {
                sh 'npm deploy'
            }
        }
 
    }
}
